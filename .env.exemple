# ── Base de données ─────────────────────────────────────────────────────────
# PostgreSQL (via docker-compose — lancer : docker compose up postgres -d)
DATABASE_URL="postgresql://wealth:wealthpass@localhost:5432/wealthtracker"

# ── Auth ────────────────────────────────────────────────────────────────────
NEXTAUTH_SECRET=""
NEXTAUTH_URL=""

# ── API prix (optionnel — fallback sur données simulées si vide) ─────────────
ALPHA_VANTAGE_API_KEY=""
COINGECKO_API_KEY=""
